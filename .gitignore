# 비밀값 (에디터 비밀번호 등) — 절대 커밋 금지
.editor-secret
.env
.env.*

# 파이썬 캐시
__pycache__/
*.pyc

# 로그
*.log

# macOS 흔적
._*
.DS_Store

# 데이터 폴더 (전역 규칙) — 루트만, fe/js/data(사이트 콘텐츠)는 추적
/data/

# 외부 참조용 다운로드 (Notion Electron 래퍼)
notion-electron/
